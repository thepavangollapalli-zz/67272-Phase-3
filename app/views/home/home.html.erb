<div class="container display">
	<h1>Welcome, chess lovers!</h1>
	<p>We aim to streamline the process of ordering wholesale chess items for local schools and chess clubs so you can focus on your main goal, teaching students. Check out our collection and make an order today!</p>
	<h3>Reorder List</h3>
	<% if @items_to_reorder.empty? %>
		<h5>No items need to be reordered!</h5>
	<% else %>
		<table>
			<thead>
				<tr>
					<th>Name</th>
					<th>Inventory Level</th>
					<th>Reorder Level</th>
					<th colspan="3"></th>
				</tr>
			</thead>
			<tbody>
			<% @items_to_reorder.each do |item| %>
				<td><%= link_to "#{item.name}", item %></td>
				<td><%= item.inventory_level %></td>
				<td><%= item.reorder_level %></td>
			<% end %>
			</tbody>
		</table>
	<% end %>
</div>